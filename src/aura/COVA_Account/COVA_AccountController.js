({
	/*goToRecords : function(component, event, helper) {
       helper.showRecordDetails(component);
    },
    
    showAllAccountTasks : function(component, event, helper){
        helper.showTasks(component);
    },*/
    
    showTasks : function(component, event, helper){
        helper.callTaskComponent(component);
    }
})